import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/copilot-ubb-budget-monitor",
  assetPrefix: "/copilot-ubb-budget-monitor/",
};

export default nextConfig;
